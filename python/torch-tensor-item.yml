rules:
- id: torch-tensor-item
  patterns:
    - pattern-either:
      - pattern: $ANYTHING = $TENSOR.item()
      - pattern: $TENSOR.item()
  message: Avoid using NumPy in PyTorch modules for efficiency and proper ONNX loading 
  languages: [python]
  severity: WARNING
